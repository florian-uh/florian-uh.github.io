document.addEventListener("DOMContentLoaded", () => {
    // Exemple simple: alerte quand on clique sur "New"
    document.querySelector(".header-buttons button").addEventListener("click", () => {
        alert("Create a new project!");
    });
});
